#include <stdio.h>
void main()
{
    int num=3;
    printf("Enter a number: \n");
    if (num > 0)
        printf("%d is a positive number \n", num);
    else if (num < 0)
        printf("%d is a negative number \n", num);
    else
        printf("zero");
}
